namespace ulam
{
    const int RIGHT = 0;
    const int UP = 1;
    const int LEFT = 2;
    const int DOWN = 3;

    void print_table(int data[], int size, int max_cols);
    int *get_value(int data[], int width, int x, int y);
}
